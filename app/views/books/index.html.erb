<%= link_to "Add a book", "/books/new"%>

<ol>
    <% @books.each do |book|%>
        <%= link_to "#{book.title}", book%> - <%= link_to "Add a comment",new_book_comment_path(book.id)%> - <%= link_to "view comments", book_comments_path(book.id)%><br>
    <%end%>
</ol>
